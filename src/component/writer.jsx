import { WriterContainer } from "./styled-elements";

const Writer = () => {
  return (
      <WriterContainer>
        <h1>this is the write content container</h1>
     </WriterContainer>
    )
};

export default Writer;
